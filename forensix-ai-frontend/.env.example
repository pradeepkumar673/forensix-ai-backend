# ForensiX AI — Vite frontend
# Copy to `.env.local` for local overrides.

# Leave UNSET in dev to use Vite proxy (same-origin → no CORS noise). Production: set full URL.
# VITE_API_BASE_URL=http://127.0.0.1:8000
